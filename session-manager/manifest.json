{
  "name": "Session Manager",
  "id": "session-manager",
  "version": "1.0.0",
  "author": "brodgar",
  "description": "One row per login the client holds: go to that character, log it out, or cycle to the next with a hotkey.",
  "api_version": 1,
  "files": ["main.lua"],
  "permissions": ["session.close"],
  "saved_variables": [{ "name": "window", "scope": "account" }]
}
