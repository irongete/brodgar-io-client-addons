{
  "id": "themes",
  "name": "Themes",
  "version": "1.0.0",
  "author": "brodgar",
  "description": "Loads whole client looks from JSON files in its own themes/ folder and installs one. Ships \"default\", which is this client's own look written out key by key -- the reference a theme of your own is edited out of. Pick one in Options > AddOns > Themes.",
  "api_version": 1,
  "files": ["main.lua"],
  "saved_variables": [{ "name": "settings", "scope": "account" }]
}
