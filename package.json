{
  "name": "brodgar-io-client-addons",
  "private": true,
  "description": "The publish tool for the addons in this repo: zips one and uploads it to brodgar.io/addons.",
  "type": "module",
  "engines": {
    "node": ">=22.2"
  },
  "scripts": {
    "publish": "node publish.js"
  },
  "bin": {
    "publish": "./publish.js"
  }
}
