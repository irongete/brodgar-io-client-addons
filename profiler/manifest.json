{
  "name": "Brodgar.io Profiler",
  "id": "profiler",
  "version": "0.6.0",
  "author": "brodgar",
  "description": "Example addon: where the frame went -- a six-tab window over hafen.client():profiling() with the frame graph and phases, render passes and GL counters, per-widget and per-addon cost, the pull-only counters and the overhead accounting. Dormant until you open it, and pausing turns the graph into a scrubbable timeline.",
  "api_version": 1,
  "permissions": ["client.settings"],
  "files": [
    "main.lua"
  ]
}
